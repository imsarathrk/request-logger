pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t imsarathrk/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag imsarathrk/request-logger:${env.BUILD_ID} imsarathrk/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push imsarathrk/request-logger:${env.BUILD_ID}"
        sh "docker push imsarathrk/request-logger:latest"
      }
    }
  }
}
